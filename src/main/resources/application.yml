spring:
  application:
    name: registry-service-sba
  boot:
    admin:
      client:
        url: http://10.0.0.188:8762
  config:
    import: optional:configserver:${CONFIG_SERVER_URL:http://spring-config.lab.pl/}

management:
  endpoints:
    web:
      exposure:
        include: "*"
  info:
    env:
      enabled: true

logging:
  level:
    com:
      netflix:
        eureka: INFO
        discovery: INFO


